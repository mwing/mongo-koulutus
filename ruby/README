To run execute 'bundle install' && 'bundle exec shotgun -p <port> config.ru'

Prerequisites:
ruby 1.8.7
rubygems 1.3.6
bundler 1.0.10

check ruby version & rubygems version, then:
gem install bundler

if you can't find bundler, make sure your GEM_HOME points to the correct location (usually ~/.gem)

