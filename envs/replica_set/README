Launch nodes:

    ./run.sh

    tail -f logs/*

Initialize replica set (run only once)

    ./replicaset_init.sh
    
Connect to replicaset:
    
    mongo # if node1 is PRIMARY
    mongo --port 37017 # if node2 is PRIMARY
    mongo --port 47017 # if node3 is PRIMARY

Before querying in SECONDARY:

    rs.slaveOk()
